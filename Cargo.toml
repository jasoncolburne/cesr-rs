[package]
name = "cesr"
version = "0.1.0"
edition = "2024"
authors = ["Jason Colburne"]
license = "MIT"
description = "Minimal CESR implementation for P-256, ML-DSA-65, ML-KEM-768, and blake3"

[lib]
name = "cesr"
path = "src/lib.rs"

[features]
default = ["native"]
native = ["rand_core/std", "blake3/std", "p256/std", "fips204/default", "fips203/default"]
wasm = ["getrandom/js"]
test-utils = []

[dependencies]
# Cryptography - use default-features = false for WASM compatibility
blake3 = { version = "1", default-features = false }
p256 = { version = "0.13", features = ["ecdsa", "ecdsa-core"], default-features = false }
fips203 = { version = "0.4", default-features = false }
fips204 = { version = "0.4", default-features = false }
signature = "2"
# RNG support - matches rand_core version used by p256/fips203/fips204
rand_core = { version = "0.6", default-features = false, features = ["getrandom"] }
getrandom = { version = "0.2", default-features = false, optional = true }

# Serialization
serde = { version = "1", features = ["derive"] }
thiserror = "1"

# Encoding
base64 = "0.22"

# WASM target dependencies
[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.2", features = ["js"] }

[dev-dependencies]
hex = "0.4"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
unwrap_in_result = "deny"
